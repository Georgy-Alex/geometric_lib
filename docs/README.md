# Math formulas
## Area
- Circle: S = πR²

**Функцию для вычисления площади круга по заданному радиусу. Функция использует значение π (пи) из стандартной библиотеки Python и возвращает площадь круга, основываясь на заданном радиусе. Решение позволяет быстро и точно вычислить площадь круга.**

**Функция area принимает один параметр r, который представляет собой радиус круга, и возвращает площадь круга, используя следующую формулу:
площадь=π×r^2.**

    Например:
        def area(3):
        return math.pi * 3 * 3 Ответ: 28.2743338823

- Rectangle: S = ab

**Функця для вычисления площади прямоугольника по заданным значениям его сторон. Функция принимает длины сторон прямоугольника и возвращает площадь, используя стандартную формулу площади.**

Параметры:

    a (int или float): Длина прямоугольника. Рекомендуется передавать положительное значение.
    b (int или float): Ширина прямоугольника. Рекомендуется передавать положительное значение.

Возвращаемое значение:

    Площадь прямоугольника с заданной длиной и шириной.

    Например:
        def area(4, 5):
        return 4 * 5 Ответ: 20

- Square: S = a²

**Функция для вычисления площади квадрата по заданной длине его стороны. Функция принимает длину стороны квадрата и возвращает площадь, используя стандартную формулу.**

Параметры:

    a (int или float): Длина стороны квадрата. Рекомендуется передавать положительное значение.

Возвращаемое значение:

    Площадь квадрата с заданной длиной стороны.

    Например:
        def area(4):
        return 4 * 4 Ответ: 16


- Triangle: S = ½ah

**Функция для вычисления площади треугольника по заданной длине основания и высоте. Функция принимает длину основания треугольника и его высоту и возвращает площадь, используя стандартную формулу.**

Параметры:

    a (int или float): Длина основания треугольника. Рекомендуется передавать положительное значение.
    h (int или float): Высота треугольника. Рекомендуется передавать положительное значение.

Возвращаемое значение:

    Площадь треугольника с заданной длиной основания и высотой.

    Например:
        def area(6, 4):
        return 6 * 4 / 2 Ответ: 12.0

## Perimeter
- Circle: P = 2πR
 
**Функция для вычисления Периметра окружности по заданному радиусу. Функция использует значение π (пи) из стандартной библиотеки Python и возвращает Периметр окружности, основанную на указанном радиусе. Решение позволяет точно вычислить периметр окружности.**

**Функция perimeter принимает один параметр r, который представляет радиус окружности, и возвращает длину окружности, используя формулу:
длина=2×π×r.**

    Например:
        def perimeter(3):
        return 2 * math.pi * 3 Ответ: 18.8495559215


- Rectangle: P = 2a + 2b

**Функция для вычисления периметра прямоугольника по заданным значениям его сторон. Функция принимает длины сторон прямоугольника и возвращает периметр, используя стандартную формулу.**

Параметры:

    a (int или float): Длина прямоугольника. Рекомендуется передавать положительное значение.
    b (int или float): Ширина прямоугольника. Рекомендуется передавать положительное значение.

Возвращаемое значение:

    Периметр прямоугольника с заданной длиной и шириной.

    Например:
        def perimeter(4, 5):
        return 2 * (4 + 5) Ответ: 18

- Square: P = 4a

**Функция для вычисления периметра квадрата по заданной длине его стороны. Функция принимает длину стороны квадрата и возвращает периметр, используя стандартную формулу.**

Параметры:

    a (int или float): Длина стороны квадрата. Рекомендуется передавать положительное значение.

Возвращаемое значение:

    Периметр квадрата с заданной длиной стороны.

    Например:
        def perimeter(4):
        return 4 * 4 Ответ: 16

- Triangle: P = a + b + c

**Этот модуль предоставляет функцию для вычисления периметра треугольника по заданным длинам его сторон. Функция принимает длины всех трех сторон треугольника и возвращает их сумму, которая представляет собой периметр.**

Параметры:

    a (int или float): Длина первой стороны треугольника. Рекомендуется передавать положительное значение.
    b (int или float): Длина второй стороны треугольника. Рекомендуется передавать положительное значение.
    c (int или float): Длина третьей стороны треугольника. Рекомендуется передавать положительное значение.

Возвращаемое значение:

    Периметр треугольника с заданными длинами сторон.

    Например:
        def perimeter(3, 4, 5):
        return 3 + 4 + 5 Ответ: 12


# Test math formulas
## Цели и задачи тестирования
### Цели

- Убедиться, что функции модуля корректно вычисляют площадь и периметр.

- Проверить корректность работы функций с различными входными данными, включая крайние и недопустимые случаи.

- Подтвердить, что модуль соответствует функциональным требованиям, таким как правильность вычислений и обработка ошибок.

### Задачи

- Провести модульное тестирование функций area и perimeter.

- Проверить стабильность и надежность работы с корректными и некорректными входными значениями. 

- Обеспечить прозрачность результатов с помощью автоматизированных тестов.

## Описание тестируемого продукта

**Модуль тестов реализует:**

- Функцияarea(a, h): вычисляет площадь.

- Функцию perimeter(a, b, c): вычисляет периметр.

**Функциональные требования:**

- Функция area должна корректно обрабатывать значения длины основания и высоты, возвращая площадь (число с плавающей точкой).

- Функция perimeter должна корректно вычислять сумму сторон треугольника, возвращая периметр (число с плавающей точкой или целое число).

## Область тестирования:

1. Проверка корректности вычислений:

- Тестирование функции area с различными наборами параметров.
- Тестирование функции perimeter с различными значениями сторон.

2. Обработка некорректных данных:

- Передача нулевых и отрицательных значений.
- Обработка нечисловых входных данных (например, строки или None).

3. Граничные случаи:

- Минимальные положительные значения параметров.
- Максимальные значения, допустимые для параметров.

4. Производительность:

-Тестирование на больших наборах входных данных.

## Стратегия тестирования

1. Модульное тестирование:

- Используется библиотека unittest.
- Тесты сосредоточены на отдельных функциях (area и perimeter).

2. Техники тестирования:

- Эквивалентное разбиение: тестирование функций с различными диапазонами данных.
- Анализ граничных значений: проверка с минимальными, нулевыми и максимальными значениями.

3. Методы тестирования:

- Позитивное тестирование: проверка корректной работы с валидными данными.
- Негативное тестирование: проверка обработки ошибок с некорректными входными данными.

## Критерии приемки

1. Функция area:

- Возвращает корректное значение площади для валидных данных.
- Выбрасывает исключения или корректно обрабатывает некорректные данные.

2. Функция perimeter:

- Возвращает корректное значение периметра для валидных данных.
- Выбрасывает исключения или корректно обрабатывает некорректные данные.

3. Тесты:

- Все тесты успешно проходят, и отчет тестирования не содержит ошибок или исключений.
- Покрытие тестами функций должно составлять не менее 90%.

## Ожидаемые результаты:

1. Отчёты о дефектах (при их наличии), содержащие описание найденных проблем, входные данные и ожидаемые результаты.

2. Автоматизированные тесты, успешно проверяющие функции модуля.

3. Статусы тестирования:

- Пройдено: Функция выполняется корректно с различными типами данных.
- Ошибка: функция возвращает неверные значения или вызывает исключение при корректных входных данных.

4. Метрики качества:

- Уровень покрытия тестами.
- Количество дефектов, найденных на этапе тестирования.

# Данные о коммитах
## Последние коммиты

- [**507bf3a**](https://github.com/Georgy-Alex/geometric_lib/commit/507bf3a83cb906ac9ffe02e80f92340bc4e9dee7) (HEAD -> new_features_464957, origin/new_features_464957): edit math and add new file
- [**d2775ba**](https://github.com/Georgy-Alex/geometric_lib/commit/d2775baa6e8b62b743092185e8dd73755230d8bf): add new file
- [**d078c8d**](https://github.com/Georgy-Alex/geometric_lib/commit/d078c8d9ee6155f3cb0e577d28d337b791de28e2#diff-0b5ca119d2be595aa307d34512d9679e49186307ef94201e4b3dfa079aa89938R10) (origin/main, origin/HEAD, main): L-03: Docs added
- [**8ba9aeb**](https://github.com/Georgy-Alex/geometric_lib/commit/8ba9aeb3cea847b63a91ac378a2a6db758682460): L-03: Circle and square added

<!-- commit e294ee73d454e57ab2033df1d977f0da8410515f
Author: Georgy-Alex <118427017+Georgy-Alex@users.noreply.github.com>
Date: Wed Oct 30 15:19:27 2024 +0300

full labaWork2

commit 507bf3a83cb906ac9ffe02e80f92340bc4e9dee7
Author: Georgy-Alex <118427017+Georgy-Alex@users.noreply.github.com>
Date: Thu Oct 3 00:54:37 2024 +0300

edit math and add new file

commit d2775baa6e8b62b743092185e8dd73755230d8bf
Author: Georgy-Alex <118427017+Georgy-Alex@users.noreply.github.com>
Date: Thu Oct 3 00:50:32 2024 +0300

add new file

commit d078c8d9ee6155f3cb0e577d28d337b791de28e2
Author: smartiqa <info@smartiqa.ru>
Date: Thu Mar 4 14:55:29 2021 +0300

L-03: Docs added

commit 8ba9aeb3cea847b63a91ac378a2a6db758682460
Author: smartiqa <info@smartiqa.ru>
Date: Thu Mar 4 14:54:08 2021 +0300

L-03: Circle and square added
 -->
